import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Template/Introduction" />

<style>
  {`
    .template-intro {
      max-width: 980px;
      padding: 24px 8px 48px;
    }

    .template-intro h1 {
      color: #051094;
      font-size: 44px;
      line-height: 1.15;
      margin-bottom: 16px;
    }

    .template-intro p {
      color: rgba(76, 78, 100, 0.78);
      font-size: 16px;
      line-height: 1.7;
    }

    .template-grid {
      display: grid;
      gap: 16px;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      margin: 28px 0;
    }

    .template-card {
      border: 1px solid rgba(76, 78, 100, 0.14);
      border-radius: 8px;
      padding: 18px;
    }

    .template-card strong {
      color: rgba(0, 0, 0, 0.9);
      display: block;
      margin-bottom: 6px;
    }

    .template-card span,
    .template-list li {
      color: rgba(76, 78, 100, 0.78);
      font-size: 14px;
      line-height: 1.6;
    }

    .template-list {
      margin: 18px 0 0;
      padding-left: 20px;
    }

    .template-code {
      background: #f7f7f9;
      border: 1px solid rgba(76, 78, 100, 0.12);
      border-radius: 8px;
      color: #212121;
      display: block;
      font-family: ui-monospace, "Cascadia Mono", "Segoe UI Mono", Menlo, Consolas, monospace;
      margin-top: 18px;
      padding: 16px;
      white-space: pre-wrap;
    }
  `}
</style>

<div className="template-intro">

# template-vite-react

This Storybook documents the reusable UI foundation included with the Vite React template. It is focused on states that are useful before an application has real product data: authenticated app shell patterns, Material UI primitives, CRUD scaffolding, upload intake, and shared feedback components.

<div className="template-grid">
  <div className="template-card">
    <strong>Protected app shell</strong>
    <span>
      Use the Dashboard stories to review the authenticated layout without
      changing auth loaders or provider behavior.
    </span>
  </div>
  <div className="template-card">
    <strong>CRUD workflow pieces</strong>
    <span>
      DataGrid list and create-form stories show how schema-driven UI can be
      extended for product records.
    </span>
  </div>
  <div className="template-card">
    <strong>Upload and feedback states</strong>
    <span>
      Dropzone, alert, loading, avatar, and MUI wrapper examples give teams
      realistic component states to copy from.
    </span>
  </div>
  <div className="template-card">
    <strong>Quality workflow</strong>
    <span>
      Storybook sits alongside the existing lint, test, app build, and Storybook
      build checks used by CI.
    </span>
  </div>
</div>

## Where To Start

<ul className="template-list">
  <li>
    Open <strong>Pages/Dashboard</strong> to see the protected app shell and
    demo dashboard composition.
  </li>
  <li>
    Open <strong>Components/CRUD</strong> for schema-driven list and form
    examples.
  </li>
  <li>
    Open <strong>Upload-UI/Components/MultiDropzone</strong> for upload intake
    states.
  </li>
  <li>
    Open <strong>Hooks/Alerts</strong> for global feedback behavior.
  </li>
</ul>

<code className="template-code">{`yarn sb
yarn build-storybook
yarn lint
yarn test`}</code>

</div>
